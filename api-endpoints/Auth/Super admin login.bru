meta {
  name: Super admin login
  type: http
  seq: 1
}

post {
  url: {{base_url}}/auth/sign-in
  body: json
  auth: none
}

body:json {
  {
  "email" : "admin@example.com",
  "password" : "adminpassword"
  }
}
